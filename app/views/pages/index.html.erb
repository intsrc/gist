<div class="container" style="margin-top:60px;background:white">
  <div class="row">
    <div class="col-12">
      <div class="snippet">
        <h4 class="snippet-title">Substring function</h4>
        <div class="snippet-history">Snippet originally posted by <span class="snippet-owner">Fort65</span> | <time>27.09.2017</time></div>
        <pre>
          &lt;?php
            public function substr_text_only($string, $limit, $end='...')
            {
                $with_html_count = strlen($string);
                $without_html_count = strlen(strip_tags($string));
                $html_tags_length = $with_html_count-$without_html_count;

                return str_limit($string, $limit+$html_tags_length, $end);
            }
            ?>
        </pre>
      </div>
      <hr>
      <div class="snippet">
        <div class="snippet-history">Gist originally posted by <span class="snippet-owner">Cadele</span> | <time>27.09.2017</time></div>
        <pre>
          &lt;?php
            public function substr_text_only($string, $limit, $end='...')
            {
                $with_html_count = strlen($string);
                $without_html_count = strlen(strip_tags($string));
                $html_tags_length = $with_html_count-$without_html_count;

                return str_limit($string, $limit+$html_tags_length, $end);
            }
            ?>
        </pre>
      </div>
    </div>
  </div>
</div>
